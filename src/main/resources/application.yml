spring:
  datasource:
    url: jdbc:mysql://localhost:3306/redakzia?serverTimezone=UTC
    username: root
    password: 9999
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization-mode: always


  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 25MB

file.upload-dir: .\\tables


user.exclepath: tables\\authors.xlsx
user.csvpath: tables\\authors.csv
